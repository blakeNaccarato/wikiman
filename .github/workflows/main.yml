on:
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@main
    - uses: actions/setup-python@main
      with:
        python-version: 3.9
    - run: |
        pip install . pytest pytest-cov
        pytest --cov
    - uses: codecov/codecov-action@1.3.2
